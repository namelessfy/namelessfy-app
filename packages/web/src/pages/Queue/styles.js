import styled from "styled-components";

const QueueContainer = styled.section`
  width: min(800px, 90%);
  margin: 0 auto;
  margin-top: 2rem;
`;
const SectionTitle = styled.h2`
  margin-left: 1rem;
  font-size: x-large;
  margin-top: 2rem;
`;
const SongList = styled.ul`
  width: 100%;
`;

export { QueueContainer, SectionTitle, SongList };
